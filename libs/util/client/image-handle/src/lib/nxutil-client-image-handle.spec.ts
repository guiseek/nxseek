import { nxutilClientImageHandle } from './nxutil-client-image-handle';

describe('nxutilClientImageHandle', () => {
  it('should work', () => {
    expect(nxutilClientImageHandle()).toEqual('nxutil-client-image-handle');
  });
});
